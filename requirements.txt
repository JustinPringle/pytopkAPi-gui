# PyTOPKAPI GUI - Dependencies
# Install with: pip install -r requirements.txt

# GUI framework
streamlit>=1.35.0
streamlit-folium>=0.20.0

# Mapping
folium>=0.16.0
geopandas>=0.14.0
shapely>=2.0.0

# DEM processing
pysheds>=0.4.0
bmi-topography>=0.9.0   # OpenTopography / SRTM download

# Raster I/O
rasterio>=1.3.0
# gdal Python bindings NOT required — system GDAL (gdalwarp, gdaldem) used via subprocess
# Install system GDAL via: brew install gdal
numpy>=1.24.0

# HDF5 (forcing files)
h5py>=3.9.0

# Plotting
plotly>=5.18.0
matplotlib>=3.7.0

# HTTP / APIs
requests>=2.31.0

# ET calculation (FAO Penman-Monteith)
scipy>=1.11.0

# Data handling
pandas>=2.0.0
xarray>=2023.6.0

# PyTOPKAPI (install separately from fork)
# pip install git+https://github.com/JustinPringle/PyTOPKAPI.git
