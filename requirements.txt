# PyTOPKAPI GUI — Dependencies
# Python 3.14+ required (macOS) / 3.11+ on Windows/Linux
# Install with: pip install -r requirements.txt
#
# NOTE (macOS): PyQt6 is supplied by Homebrew, NOT from PyPI.
# Add the Homebrew site-packages to the venv via:
#   echo "/opt/homebrew/lib/python3.14/site-packages" \
#     > .venv/lib/python3.14/site-packages/homebrew-pyqt6.pth
#
# NOTE: Install PyTOPKAPI separately from the fork:
#   pip install git+https://github.com/JustinPringle/PyTOPKAPI.git

# ── GUI framework ──────────────────────────────────────────────────────────────
# PyQt6 >=6.7 (macOS: from Homebrew; Windows/Linux: pip install PyQt6)
PyQt6>=6.7.0
PyQt6-WebEngine>=6.7.0          # QWebEngineView for interactive Folium maps

# ── Mapping ────────────────────────────────────────────────────────────────────
folium>=0.16.0
geopandas>=1.0.0
shapely>=2.0.0
pyproj>=3.6.0

# ── DEM / raster processing ────────────────────────────────────────────────────
pysheds>=0.4.0
rasterio>=1.3.0
numpy>=1.24.0
# System GDAL required for gdalwarp / gdaldem (subprocess calls):
#   macOS:   brew install gdal
#   Ubuntu:  apt install gdal-bin
#   Windows: OSGeo4W or conda-forge

# ── HDF5 (forcing files + results) ────────────────────────────────────────────
h5py>=3.9.0

# ── Plotting (raster canvas + hydrograph) ─────────────────────────────────────
matplotlib>=3.8.0

# ── HTTP / APIs (OpenTopography DEM download) ─────────────────────────────────
requests>=2.31.0

# ── Data handling (forcing CSV/Excel import) ──────────────────────────────────
pandas>=2.0.0
openpyxl>=3.1.0       # Excel support for pandas

# ── Network graph (create_file.py uses networkx for flow routing) ─────────────
networkx>=3.0.0

# ── Miscellaneous ─────────────────────────────────────────────────────────────
scipy>=1.11.0         # FAO Penman-Monteith ET (future)

# ── Packaging (development only) ──────────────────────────────────────────────
# pyinstaller>=6.0.0  # uncomment when building distributable .app / .exe
